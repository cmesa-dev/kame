[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "kame-ops-demo"
version = "1.0.0"
description = "Auditable local workflow agent demo for operational incident briefs"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
kame-demo = "kame.cli:main"

[tool.setuptools.packages.find]
include = ["kame*"]
